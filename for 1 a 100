<# Programa para exibir todos os números pares de 1 a 100

# Laço de repetição de 1 até 100 (inclusive)
for numero in range(1, 101):
    # Verifica se o número é par usando o operador módulo %
    if numero % 2 == 0:
        print(numero)>
